import { Input } from './input';

const SearchInput = () => {
  const changeHandler = () => {
    
  };
  return <Input onChange={changeHandler} placeholder="Search" />;
};

export default SearchInput;
