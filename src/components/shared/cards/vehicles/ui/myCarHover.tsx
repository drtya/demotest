import { UserIcon } from '@heroicons/react/24/outline';
import React from 'react';

type Props = {};

const CarCreator = (props: Props) => {
  return <UserIcon width={24} height={24} />;
};

export default CarCreator;
